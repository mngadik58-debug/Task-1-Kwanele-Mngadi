Garbage in, garbage out. Every dashboard, model, and decision fails if the data underneath is messy. 

**Project 1 is your foundation phase.** This isn’t about fancy charts. It’s about mastering data integrity. I took raw, messy datasets and scrubbed them into a reliable source of truth , because insights are only as good as the data they’re built on.

# What I Did
- **Handled missing values**: Identified gaps + applied the right fill/drop strategy
- **Eliminated duplicates**: Removed redundant rows that skew results  
- **Corrected formatting**: Fixed data types, dates, and inconsistent entries
